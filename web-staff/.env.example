# Origin of the backend API. Leave unset for local development — the vite dev
# server proxies /api and /uploads to http://localhost:4000.
# Set this in the Vercel project's environment variables when deploying:
# VITE_API_BASE_URL=https://car-rental-api.vercel.app
